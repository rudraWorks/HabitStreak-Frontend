import React from 'react';

const BetaTestingBadge = () => {
  return (
    <span style={{ display: 'inline', padding: '1px 2px', borderRadius: '5px', background: '#007bff', color: '#fff',fontSize:'.8rem' , position:'relative',top:'-2px'}}>
        beta
    </span>
  );
};

export default BetaTestingBadge;
